import type { InAdminHeaderConfig } from "@ingot/admin-core";

/**
 * 顶栏集中配置。默认保留全屏、设置与现有用户菜单。
 * 大类入口、自定义搜索/小部件等示例见仓库 docs/app-header.md。
 */
export const createAppHeader = (): InAdminHeaderConfig => ({});
