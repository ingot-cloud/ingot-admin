# 在线示例

演示运行在独立 iframe 中，源码位于 `apps/dev-portal/demos`，与页面嵌入同源。使用固定数据和最小上下文，不登录、不请求业务网关。iframe 与站点同步明暗，并按内容高度调整。

## 按钮

<DemoFrame demo-id="in-button" />

## 标题

<DemoFrame demo-id="in-title" />

## 标签

<DemoFrame demo-id="in-tag" />

## 表格筛选约定

<DemoFrame demo-id="in-table" />
