# 任务：顶栏五区布局与 APP 配置扩展

## 准备

- [x] 用户明确确认开工后，将 README 状态从 draft 改为 approved，开工时改为 implementing。
- [x] 阅读本 change 的 REQUIREMENTS.md、DESIGN.md，以及 CONSTITUTION、in-coding-standards；本期无 API.md，不读取 inbox。
- [x] 检查当前主题和顶栏实现，确认全局路由标签栏拆除变更边界，不修改其工件。

## 配置与共享能力

- [x] 增加 header 及相关公开类型、默认值解析、响应式读取和稳定 key 校验；组件及回调仅经应用注入传递。
- [x] 在 useAdminShell 公开 header 读取能力，完成包入口导出与自定义主题兼容。
- [x] 完成旧 utilities prop、shellSlots 和 InAppBar 插槽适配，保留默认应用行为。

## 五区与交互

- [x] 拆分五区布局；品牌宽度与侧栏 Token、内边距、动画一致，收起隐藏标题，适配 overlay 和无侧栏场景。
- [x] 实现大类直接入口、受控选中态、分组多列面板、更多浮层及分组返回交互。
- [x] 实现可测试的宽度分配算法与 ResizeObserver 调度，优先保留选中项，处理文字及动态尺寸变化。
- [x] 接入内置/自定义小部件、角标、显隐、禁用、排序与从右向左收纳，确保组件实例不重复或丢失。
- [x] 接入搜索文案与自定义组件，保持原显隐源，完成图标/浮层紧凑模式及输入状态保留。
- [x] 实现用户独立区域及入口扩展，内置菜单可选/可排序/可隐藏，自定义动作和分隔线，处理空菜单及异步动作。
- [x] 完成窄屏整体降级、浮层限宽限高、键盘操作、Escape 关闭和焦点恢复。

## APP 示例与文档

- [x] APP 增加集中顶栏配置入口，保持正式默认功能，不添加无业务能力的入口。
- [x] 文档示例展示分组菜单、受控选中、自定义搜索/小部件、用户菜单排序隐藏追加，以及未来 APP 侧栏联动接入点。
- [x] 更新主题接入与兼容说明，明确列表替换语义、插槽兼容和组件状态契约。

## 验证

- [x] 单元/组件测试：默认值、空数组、响应式显隐、配置顺序、禁用项、内置用户操作和自定义回调。
- [x] 单元/组件测试：宽度边界、更多按钮占位、选中项保留、动态内容、旧 props/插槽兼容、浮层焦点与组件实例保留。
- [ ] 真实浏览器验证 1440、1280、1024、768、375px；展开/收起侧栏、搜索开关、长标题、多小部件、分组面板、浅色/深色主题。
- [ ] 确认品牌边界对齐，无重叠/整页横向滚动/宽度抖动，收纳功能均可访问。
- [x] 执行 pnpm build:packages、相关 APP 构建、类型检查、相关 Vitest、只读 lint，以及 check:boundaries、check:examples、check:docs。
- [ ] 对照 REQUIREMENTS.md 逐项记录验收结果，将状态改为 validating。

## 收尾

- [ ] 验收通过后更新 current/packages/admin-theme 与 admin-ui-foundation 的页面行为规格，不提前更新。
- [ ] 在对应 capability README 记录变更 ID 并链接归档规格；本期无 API.md，不添加无效接口链接。
- [ ] README 补齐完成记录、状态改为 completed，将整个 change 移至 changes/archive/2026/。
