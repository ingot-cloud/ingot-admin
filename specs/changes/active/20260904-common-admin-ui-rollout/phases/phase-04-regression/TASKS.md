# Phase 04：全量回归

- [ ] 对照页面矩阵确认 25 个 `IndexPage.vue` 均已迁移且无遗漏。
- [ ] 对照迁移前清单验证路由、菜单、canonical viewPath、权限和接口行为零回归。
- [ ] 检查所有筛选、表格、分页、局部 Tab、双栏、抽屉、弹窗和页面内编辑态。
- [ ] 检查每个页面使用正确的 page/contained 模式，未通过页面根 `overflow` 绕过滚动契约。
- [ ] 检查列表页面头、工具栏、表头和分页固定，数据区独立滚动；双栏可折叠且左右独立滚动。
- [ ] 检查行内/工具栏 `…`、快捷操作数量、工具栏原子折叠组、危险确认、稳定 `tableId` 和字段显示偏好。
- [ ] 检查空数据、无结果、错误、无权限、loading、disabled 和只读状态。
- [ ] 检查危险操作对象信息、影响说明、防重复提交和成功后的局部刷新。
- [ ] 在 1440×900、1280×800、1024×768 和窄窗口执行视觉回归。
- [ ] 执行键盘导航、焦点、Tooltip、必填/错误表达和颜色对比度检查。
- [ ] 执行 plugin type-check/test、admin build、lint 和 boundaries 检查。
- [ ] 将状态改为 `validating`，等待用户全量验收，不提前修改 current。
