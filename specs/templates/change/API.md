# 接口：<feature>

<!-- 变更的功能名称。inbox 已有完整后端接口文档时，移入原文即可，不必按本模板重写。 -->

## 来源

<!-- 后端 change 路径、自行编写、或原始文件名 -->

- 原始文件：
- 后端仓库：

## 服务与约定

- **服务**：
- **Base Path**：
- **鉴权**：
- **响应包装**：统一 `R<T>`（`code` / `message` / `data`），`code = "0"` 为成功

## 权限码

| 权限码 | 覆盖接口 |
|--------|----------|
|        |          |

## 接口列表

### 1. `<METHOD>` `<path>`

**权限**：

**说明**：

**请求参数**：

| 位置 | 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
|      |      |      |      |      |

**响应 `data`**：

```json
{}
```

## 前端注意

<!-- 时间格式、分页语义、枚举展示、与旧接口的差异等 -->
